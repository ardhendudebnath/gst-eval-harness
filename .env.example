# Copy to .env and fill in. Only needed from week 4 (model runs) onward;
# the dataset tooling needs no keys.
#
# .env is git-ignored. Never commit real keys.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=

# Open-weight tier (Llama / Qwen / Mistral) without self-hosting.
OPENROUTER_API_KEY=
